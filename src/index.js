import { createDOM, projectModal, renderAll, renderImportant, renderTodayArray, renderWeekArray } from "./dom";
 

createDOM(); 
projectModal(); 
renderAll(); 
renderImportant(); 
renderTodayArray(); 
renderWeekArray();






